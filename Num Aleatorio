#include <stdio.h>
#include <stdlib.h>
#include<time.h>
int main(int argc, char const*argv[])
{
    srand(time(0));
    for (int i=0; i<5; i++)
    {
        int random= rand()%10;
        printf("%d\n", random%10);
    }
    return 0;
}
